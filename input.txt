
x := 2 - 4 / (3 + 1);

if true and true and !false then
    skip;
else
    while false or !true or true do
        skip;
    od
fi

while true do
    read x, y;

    if x <= 1 or !(x > 8) and x != y or false then
        write x, y;
        skip;
    else
        z := (x + y) / 2;
        write z;
    fi

    zero := 0;
    write zero, zero;
od
